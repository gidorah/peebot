---
description: Ask a question about the codebase without making changes or running commands.
---
1. Analyze the user's request to understand what information is needed.
2. Use read-only tools (like `view_file`, `grep_search`, `find_by_name`, `list_dir`, `read_url_content`) to gather the necessary information.
3. DO NOT use tools that modify files (`write_to_file`, `replace_file_content`, etc.) or run commands (`run_command`).
4. Synthesize the gathered information into a clear and helpful response.
